# Example: Build from Source Control (Azure DevOps)
# Builds a DBmaestro package from source control by task list, commit hash, or all changes.
# Trigger manually via the Azure DevOps UI and set the required parameters.
resources:
  repositories:
    - repository: dbmaestro-cicd
      type: github
      name: DBMaestroDev/dbmaestro-cicd
      endpoint: dbmaestro-cicd  # Replace with your GitHub service connection name if different
      ref: refs/tags/v1

parameters:
  - name: packageName
    displayName: 'Package name to build'
    type: string
  - name: projectName
    displayName: 'DBmaestro project name'
    type: string
    default: 'Demo-PSQL'
  - name: environmentName
    displayName: 'Development environment name'
    type: string
    default: 'Dev_Env_1'
  - name: buildType
    displayName: 'What to include in the package'
    type: string
    values:
      - 'Specific Tasks'
      - 'Specific Commit'
    default: 'Specific Tasks'
  - name: tasksList
    displayName: 'Task IDs (comma-separated, optional - used when buildType = Specific Tasks)'
    type: string
    default: 'none'
  - name: commitHash
    displayName: 'Commit hash (optional - used when buildType = Specific Commit)'
    type: string
    default: 'none'
  - name: createDowngradeScripts
    displayName: 'Create downgrade scripts'
    type: string
    values:
      - 'False'
      - 'True'
    default: 'False'
  - name: agentJarPath
    type: string
    default: '$(Pipeline.Workspace)/DBmaestroAgent.jar'
  - name: runnerPool
    type: string
    default: 'dbmaestro-linux'

trigger: none
pr: none

stages:
  - stage: Setup
    displayName: 'Setup'
    jobs:
      - job: GetCliJar
        displayName: 'Download DBmaestro CLI'
        pool: ${{ parameters.runnerPool }}
        steps:
          - checkout: dbmaestro-cicd
            fetchDepth: 1

          
          - template: azure-devops/templates/get-cli-jar.yml@dbmaestro-cicd
            parameters:
              jarPath: ${{ parameters.agentJarPath }}
              useWindows: false
              version: "26.1.2.13455"

  - stage: Build
    displayName: 'Build Package from Source Control'
    dependsOn: Setup
    jobs:
      - job: BuildFromSource
        displayName: 'Build Package - ${{ parameters.packageName }}'
        pool: ${{ parameters.runnerPool }}
        steps:
          - checkout: self
            fetchDepth: 1

          - checkout: dbmaestro-cicd
            fetchDepth: 1

          - bash: |
              case "${{ parameters.buildType }}" in
                "Specific Tasks")
                  if [ "${{ parameters.tasksList }}" = "none" ] || [ -z "${{ parameters.tasksList }}" ]; then
                    echo "##vso[task.logissue type=error]buildType is 'Specific Tasks' but tasksList is not provided"
                    exit 1
                  fi
                  echo "##vso[task.setvariable variable=versionType]Tasks"
                  echo "##vso[task.setvariable variable=additionalInfo]${{ parameters.tasksList }}"
                  ;;
                "Specific Commit")
                  if [ "${{ parameters.commitHash }}" = "none" ] || [ -z "${{ parameters.commitHash }}" ]; then
                    echo "##vso[task.logissue type=error]buildType is 'Specific Commit' but commitHash is not provided"
                    exit 1
                  fi
                  echo "##vso[task.setvariable variable=versionType]Specific Commit"
                  echo "##vso[task.setvariable variable=additionalInfo]${{ parameters.commitHash }}"
                  ;;
              esac
            displayName: 'Resolve Build Type Parameters'

          - template: azure-devops/templates/build-from-source.yml@dbmaestro-cicd
            parameters:
              packageName: ${{ parameters.packageName }}
              projectName: ${{ parameters.projectName }}
              envName: ${{ parameters.environmentName }}
              versionType: $(versionType)
              additionalInformation: $(additionalInfo)
              agentJarPath: ${{ parameters.agentJarPath }}
              server: $(DBMAESTRO_SERVER)
              user: $(DBMAESTRO_USER)
              password: $(DBMAESTRO_PASSWORD)
              useSsl: 'True'
              authType: 'DBmaestroAccount'
              createDowngradeScripts: ${{ parameters.createDowngradeScripts }}
              useWindows: false
